# Dépendances principales pour FastWizard
typer[all]>=0.20.0
rich>=14.0.0

# Dépendances pour les tests
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0

# Dépendances optionnelles pour les modules générés
# (incluses dans les projets générés mais pas nécessaires pour FastWizard lui-même)
# fastapi>=0.104.0
# uvicorn[standard]>=0.24.0
# sqlalchemy>=2.0.0
# alembic>=1.12.0
# python-jose[cryptography]>=3.3.0
# passlib[bcrypt]>=1.7.4
# python-multipart>=0.0.6
# aiofiles>=23.0.0
# loguru>=0.7.0
# psycopg2-binary>=2.9.0
# pymysql>=1.1.0
